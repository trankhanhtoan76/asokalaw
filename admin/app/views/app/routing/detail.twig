{% extends "app/base.twig" %}
{% block content %}
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2 mt-2">
                <div class="col-md-9">
                    {{ breakcrum('Routing',app.record.name)|raw }}
                </div>
                <div class="col-md-3 text-left text-md-right mt-2 mt-md-0">
                    <div class="btn-group">
                        <a href="{{ base_url }}/admin/routing/{{ app.record.id }}/edit" type="button" class="btn btn-primary btn-flat">Sửa</a>
                        <button type="button" class="btn btn-primary btn-flat dropdown-toggle dropdown-icon" data-toggle="dropdown">
                            <div class="dropdown-menu dropdown-menu-right dropdown-menu-md-left" role="menu">
                                <a class="dropdown-item" onclick="window.location.href='{{ base_url }}/admin/routing/layout/create'">Tạo mới</a>
                                <a class="dropdown-item" onclick="window.location.href='{{ base_url }}/admin/routing'">Danh sách</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" onclick="deleteRecord('routing','{{ app.record.id }}')">Xóa</a>
                            </div>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <section class="content">
        <div class="container-fluid">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">{{ lang('LBL_GENERAL_INFO') }}</h3>
                </div>
                <div class="card-body">
                    <div class="row">
                        {% include  'app/fields/text/view.twig' with {'name':'name','def':app.db.def.routing.name,'value':app.record.name} only %}
                        {% include  'app/fields/text/view.twig' with {'name':'target','def':app.db.def.routing.target,'value':app.record.target} only %}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}